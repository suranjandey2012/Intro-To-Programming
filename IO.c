#include<stdio.h>
#include<math.h>
#include<string.h>

void main(int argc,char **argv)
{
	
   //char str[]="Hello World";
   //puts(str);                 //puts(const char *s) adds a newline at the end. 
   //printf("New String");
   //int c;
   //while(EOF!=(c=fgetc(stdin)))
   //	printf("%c",c);
   /*char str[100];
   while(NULL!=fgets(str,sizeof(str),stdin))
	   puts(str);*/
   /*
   printf("%lf\n",log10(100));
   printf("%lf\n",log(2));
   //printf("%lf",(-199/232));
   double d=(-432/233);
   printf("%lf %lf\n",d,fabs(d));
   double a=7,b=4;
   printf("%lf\n",fmod(a,b));  //fmod(a,b), modf(d2,&integer);
   printf("%lf\n",sqrt(a));
   
   double integer;
   double d2=8.13265726;
   double fract=modf(d2,&integer);
   printf("%lf %lf\n",fract,integer);
   
   double d3=7.546362;
   fract=modf(d3,&integer);
   printf("%lf %lf",fract,integer);*/
   
   /*FILE *fptr;
   if(NULL==(fptr=fopen(argv[1],"w")))
	   printf("ERROR");
   
   /*int c;
   while(EOF!=(c=fgetc(fptr)))
	   printf("%c",c);*/
   
   char buffer[100]="This is a string";

   /*while(NULL!=fgets(str,sizeof(str),fptr)){
	   printf("%s",str);
	   printf("This is a separator\n");*/
	   
   /*while(1==fscanf(fptr,"%s",buffer)){
       puts(buffer);*/
    /*fread(buffer,sizeof(buffer),1,fptr);	   
    puts(buffer);
    fwrite(buffer,sizeof(buffer),1,fptr);
    fclose(fptr);*/

   //double d=6.2;
   //printf("%-7.2lf",d);
   
   /*FILE *fptr;
   if(NULL==(fptr=fopen(argv[1],"w")))
	   printf("ERROR");
   char b[100];
   //fread(b,sizeof(b),1,fptr);
   //puts(b);
   fwrite(buffer,sizeof(buffer),1,fptr);
   fclose(fptr);*/
   
   /*char *token;
   token=strtok(buffer," ");
   
   while(NULL!=token)
   {
	   puts(token);
	   token=strtok(NULL," ");
   }*/


    
	
   }
       

	
 
   
   
   